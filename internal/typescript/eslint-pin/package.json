{
  "name": "bulwark-eslint-pin",
  "version": "0.0.0",
  "private": true,
  "description": "Version pins for bulwark's bundled ESLint toolchain. Not a package bulwark builds or publishes: it exists so Dependabot can see these versions and open bump PRs, and so `npm ci` installs them deterministically. See internal/typescript/typescript.go.",
  "dependencies": {
    "@typescript-eslint/parser": "8.63.0",
    "eslint": "10.8.1",
    "eslint-plugin-security": "4.0.1",
    "typescript": "7.0.2"
  }
}
